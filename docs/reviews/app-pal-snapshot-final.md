# App / Today / Pal runtime final scoped review

## Verdict

PASS for the pending runtime integration and generation-context handoff on top of storage commit `379b741`. This is not full catalog-closure or whole-project acceptance. Production code was reviewed read-only, against the snapshot closure contract first and implementation quality second. The verifier added browser coverage and corrected the earlier report; no production fix was authored during this review, so the production review remains independent. The verifier-authored browser test itself is not independently reviewed.

## Spec trace

- App reads selected snapshot recipe/raw counts and manifest identity/verification metadata. No hardcoded count or current-game compatibility claim remains in its status.
- Today and Shopping both use `planRuntimeWorkspace`; Today labels unresolved goals and partial totals. The full-App browser case changes same-ID recipe inputs, keeps the goal's old binding, reloads, and compares Today missing-type count with the rendered Shopping list.
- Workspaces enumerates using `runtime.selected.pals`, captures `{snapshotId: runtime.metadata.selectedCatalog, revision: runtime.metadata.revision}` in the same render, and hands that object to `store.saveRoute(..., generation)`. It does not reread metadata at save-click time. Existing checklist edits omit generation and retain their binding.
- Storage's committed API rejects absent or mismatched new-route generation inside the shared write transaction. Its selected snapshot also feeds Pal/base validation. Existing imported/retired IDs remain editable without admitting new unsupported IDs or weakening shape constraints.
- Saved route names and warnings resolve the exact saved binding. Legacy/missing bindings pass `null`, not the helper's bundled default, and disable checklist completion. Saved routes remain rendered when selected bytes are missing. Bases explicitly use the selected analysis context without inventing stored bindings.
- Migration preview exposes route keep/migrate controls, before/after warnings and base coverage/suitability. Existing graph/checklist data is not regenerated by binding migration.

## Quality and executable evidence

- `npm run typecheck`: PASS.
- Targeted ESLint over the five production runtime files, snapshot unit test, dedicated browser test and config: PASS, zero warnings.
- `npm test -- src/features/pals src/features/today-guild.test.tsx src/features/today-actions.test.tsx src/data/catalog-migration.test.ts`: PASS, 11 files / 106 tests. This includes the storage fencing cases (selected rules, missing selection, ABA, unrelated revision changes) and the separately owned Today fixtures; that fixture file was not edited or staged here.
- `npx playwright test --config playwright.pal-snapshot.config.ts`: PASS, 2 Chromium tests. Dedicated strict port 4293, fresh contexts. New test observes the actual UI-to-storage generation argument, pauses that UI save, migrates in a second tab, releases the call, and verifies the real transaction rejects without route or metadata writes. A fresh render then saves successfully; the second tab reads back the exact accepted binding.
- `npx playwright test --config playwright.pal-storage-fencing.config.ts`: PASS, 1 Chromium two-tab storage test, dedicated strict port 4294.
- No default user database reset, production reference change, secret edit or unrelated staging. Browser fixtures use fresh ephemeral contexts; unit Pal stores use isolated names.

The first new browser run used an overly strict label query; the accessible-role selector fixed it. A subsequent attempt captured the initial pre-fixture runtime revision while asynchronous subscriptions were settling. Reloading after fixture seeding establishes the initial render being asserted; the actual second-tab race still happens after the UI save captures its generation. These were test setup corrections, not product fixes. No mutation-verifier PASS is claimed from any earlier failed patch; the successful browser test inspects the real save-call argument directly.

## Remaining parent acceptance boundaries

- Migration comparison display reads personal rows after preview capture. A concurrent write between those reads can produce a transient mixed-revision comparison; accept remains revision-fenced and cannot commit that stale proposal. This existing display-consistency gap is not closed by the runtime handoff and should be resolved before claiming the full atomic-preview display contract.
- No full browser migration-panel keep/migrate/cancel journey was added here; its route/base comparisons have domain/storage coverage, not complete panel interaction coverage.
- App's craft workspace data and catalog runtime are separate subscriptions/state; this review does not establish whole-App cross-tab freshness of craft records. Full acceptance remains the parent's responsibility.
